#include<vector>
using namespace std;
struct matrix{
 int m;
};

//dec for compare
bool compare(matrix&,matrix&);

//dec for change_val
vector<int> change_val(int,vector<int>);

int main(){
 matrix m;
 matrix m1;
 vector<int> v,vout;
 int i;
 compare(m,m1);
 vout=change_val(i,v);
}

bool compare(matrix& mt,matrix& mt1){
 return true;
}

vector<int> change_val(int i,vector<int> v){
 vector<int> out;
 return out;
}
